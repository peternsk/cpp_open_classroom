#include <iostream>
#include <string>

using namespace std;

int main()
{
    // int ageUtilisateur(16);
    // int nombreAmis(432);

    // double pi(3.1416);

    // bool estMonAmi(true);

    // char lettre('a');

    string nomUtilisateur ("Albert Einstein");

    unsigned int qiUtilisateur(150);

    cout << "votre nom est : " << nomUtilisateur << "est votre Qi est de : " << qiUtilisateur << endl;

    return 0;
}
